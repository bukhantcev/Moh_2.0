from django.apps import AppConfig

class DepartmentEventsConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'department_events'
    verbose_name = 'События по подразделениям'
